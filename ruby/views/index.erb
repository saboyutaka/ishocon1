<!DOCTYPE html>
<html>
<head>
<meta http-equiv="Content-Type" content="text/html" charset="utf-8">
<link rel="stylesheet" href="/css/bootstrap.min.css">
<title>すごいECサイト</title>
</head>

<body>
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">すごいECサイトで爆買いしよう!</a>
    </div>
    <div class="header clearfix">
    <% if current_user %>
      <nav>
        <ul class="nav nav-pills pull-right">
          <li role="presentation"><a href="/users/<%= current_user[:id] %>"><%= current_user[:name] %>さんの購入履歴</a></li>
          <li role="presentation"><a href="/logout">Logout</a></li>
        </ul>
      </nav>
    <% else %>
    <nav>
      <ul class="nav nav-pills pull-right">
        <li role="presentation"><a href="/login">Login</a></li>
      </ul>
    </nav>
    <% end %>
  </div>
</nav>

<div class="jumbotron">
  <div class="container">
    <h1>今日は大安売りの日です！</h1>
  </div>
</div>
<div class="container">
  <div class="row">
    <% products.each do |product| %>
      <div class="col-md-4">
        <div class="panel panel-default">
          <div class="panel-heading">
            <a href="/products/<%= product[:id] %>"><%= product[:name] %></a>
          </div>
          <div class="panel-body">
            <a href="/products/<%= product[:id] %>"><img src="<%= product[:image_path] %>" class="img-responsive" /></a>
            <h4>価格</h4>
            <p><%= product[:price] %>円</p>
            <h4>商品説明</h4>
            <p><%= product[:description][0..69] %>…</p>
            <% comments = comments_by_product_id product[:id] %>
            <h4><%= comments.size %>件のレビュー</h4>
            <ul>
              <% comments.take(5).each do |comment| %>
                <li><%= comment[:content].size > 25 ? "#{comment[:content][0..24]}…" : comment[:content] %> by <%= find_user(comment[:user_id])[:name] %></li>
              <% end %>
            </ul>
          </div>
          <% if current_user %>
            <div class="panel-footer">
              <form method="POST" action="/products/buy/<%= product[:id] %>">
                <fieldset>
                  <input class="btn btn-success btn-block" type="submit" name="buy" value="購入" />
                </fieldset>
              </form>
            </div>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>
</div>

</body>
</html>
